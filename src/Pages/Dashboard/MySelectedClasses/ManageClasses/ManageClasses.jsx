import React from 'react';

const ManageClasses = () => {
    // const {} = useQuery(['users', async() => {
    //     const res = await fetch('')
    // }])
    return (
        <div>
            
        </div>
    );
};

export default ManageClasses;